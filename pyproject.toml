[tool.poetry]
name = "aestatic"
version = "0.1.0"
description = ""
authors = ["Günther Jena <guenther@jena.at>"]

[tool.poetry.dependencies]
python = "^3.6"

[tool.poetry.dev-dependencies]

[tool.poetry.scripts]
aestatic = 'aestatic:__main__'

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
